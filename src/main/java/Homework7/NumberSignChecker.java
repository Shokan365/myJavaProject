package Homework7;

public class NumberSignChecker // 2. Print if number is positive or negative
public class NumberSignChecker {
    public static void checkNumber(int number) {
        System.out.println(number >= 0 ? "Positive" : "Negative");
    }
}{
}
